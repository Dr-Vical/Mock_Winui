<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RswareDesign.Controls.CompareParameterPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:RswareDesign.Models">

    <Border Background="{StaticResource GlassBrush}"
            BorderBrush="{StaticResource GlassBorderBrush}"
            BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Padding="10,6"
                  BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PanelLabelText" FontSize="13" FontWeight="Bold"
                           VerticalAlignment="Center" />
                <TextBlock x:Name="NodeNameText" Grid.Column="1" FontSize="11"
                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                           VerticalAlignment="Center" Margin="8,0,0,0"
                           TextTrimming="CharacterEllipsis" />
                <Button x:Name="CloseBtn" Grid.Column="2"
                        FontSize="10" Background="Transparent" BorderThickness="0"
                        Padding="6,2" MinWidth="24" MinHeight="24"
                        Click="OnCloseClick">
                    <FontIcon Glyph="&#xE711;" FontSize="10" />
                </Button>
            </Grid>

            <!-- Column Headers -->
            <Grid Grid.Row="1" Background="{StaticResource GridHeaderBrush}" Height="28">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="60" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="FT NUM" Style="{StaticResource CaptionTextBlockStyle}"
                           Padding="8,0" VerticalAlignment="Center" FontWeight="SemiBold" />
                <TextBlock Grid.Column="1" Text="PARAMETER" Style="{StaticResource CaptionTextBlockStyle}"
                           Padding="8,0" VerticalAlignment="Center" FontWeight="SemiBold" />
                <TextBlock Grid.Column="2" Text="VALUE" Style="{StaticResource CaptionTextBlockStyle}"
                           Padding="8,0" VerticalAlignment="Center" FontWeight="SemiBold" />
                <TextBlock Grid.Column="3" Text="UNITS" Style="{StaticResource CaptionTextBlockStyle}"
                           Padding="8,0" VerticalAlignment="Center" FontWeight="SemiBold" />
            </Grid>

            <!-- Data Rows -->
            <ListView Grid.Row="2" x:Name="ParamListView"
                      SelectionMode="Single" Background="Transparent">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="MinHeight" Value="28" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ParameterItem">
                        <Grid Height="28" BorderBrush="{StaticResource DividerBrush}"
                              BorderThickness="0,0,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="60" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind FtNumber}" Padding="8,0"
                                       VerticalAlignment="Center" FontSize="11"
                                       FontFamily="Cascadia Code, Consolas" />
                            <TextBlock Grid.Column="1" Text="{x:Bind Name}" Padding="8,0"
                                       VerticalAlignment="Center" FontSize="11"
                                       FontFamily="Cascadia Code, Consolas" />
                            <TextBlock Grid.Column="2" Text="{x:Bind Value}" Padding="8,0"
                                       VerticalAlignment="Center" FontSize="11"
                                       FontWeight="SemiBold" FontFamily="Cascadia Code, Consolas"
                                       Foreground="{StaticResource AccentBrush}" />
                            <TextBlock Grid.Column="3" Text="{x:Bind Unit}" Padding="8,0"
                                       VerticalAlignment="Center" FontSize="11"
                                       FontFamily="Cascadia Code, Consolas"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>
